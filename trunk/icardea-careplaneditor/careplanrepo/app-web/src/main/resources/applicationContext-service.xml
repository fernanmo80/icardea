<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd">

	<bean id="helloWorldService" class="tr.com.srdc.icardea.service.impl.HelloWorldServiceImpl" />

<!-- Hibernate SessionFactory -->
	<bean id="sessionFactory"
		class="org.springframework.orm.hibernate3.annotation.AnnotationSessionFactoryBean">
		<property name="dataSource" ref="dataSource" />
		<property name="annotatedClasses">
			<list>
				<value>tr.com.srdc.icardea.careplan.pojo.Medicalcareplantemplate</value>
				<value>tr.com.srdc.icardea.careplan.pojo.Patient</value>
				<value>tr.com.srdc.icardea.careplan.pojo.Personalizedmedicalcareplan</value>
			</list>
		</property>
		<property name="hibernateProperties">
			<props>
				<!-- Your database is MySQL 5.1 -->
				<prop key="hibernate.dialect">org.hibernate.dialect.MySQL5Dialect</prop>
				<!-- You can also use org.hibernate.dialect.MySQL5InnoDBDialect if all your tables are InnoDBs.-->
	
				<prop key="hibernate.query.substitutions">Y</prop>
				<prop key="hibernate.show_sql">true</prop>
				<!-- Turn batching off for better error messages under PostgreSQL 
					<prop key="hibernate.jdbc.batch_size">0</prop> -->
			</props>
		</property>
	</bean>
	
	<bean id="dataSource" class="org.apache.commons.dbcp.BasicDataSource"
		destroy-method="close">
		<property name="driverClassName" value="${jdbc.driverClassName}" />
		<property name="url" value="${jdbc.url}" />
		<property name="username" value="${mysql.username}" />
		<property name="password" value="${mysql.password}" />
		<property name="maxActive" value="100" />
		<property name="maxWait" value="1000" />
		<property name="poolPreparedStatements" value="true" />
		<property name="defaultAutoCommit" value="true" />
		<property name="testOnBorrow" value="true" />
		<property name="validationQuery" value="select 1=1" />
	</bean>


	<bean id="myHibernateInterceptor"
		class="org.springframework.orm.hibernate3.HibernateInterceptor">
		<property name="sessionFactory">
			<ref local="sessionFactory" />
		</property>
	</bean>

	<!-- Transaction manager for a single Hibernate SessionFactory (alternative to JTA) -->
	<bean id="transactionManager"
		class="org.springframework.orm.hibernate3.HibernateTransactionManager">
		<property name="sessionFactory" ref="sessionFactory" />
	</bean>

	<bean id="medicalcareplantemplateDAO" class="tr.com.srdc.icardea.careplan.hibernate.MedicalcareplantemplateHibernateDAO">
		<property name="sessionFactory" ref="sessionFactory" />
	</bean>

	<bean id="patientDAO" class="tr.com.srdc.icardea.careplan.hibernate.PatientHibernateDAO">
		<property name="sessionFactory" ref="sessionFactory" />
	</bean>

	<bean id="personalizedmedicalcareplanDAO" class="tr.com.srdc.icardea.careplan.hibernate.PersonalizedmedicalcareplanHibernateDAO">
		<property name="sessionFactory" ref="sessionFactory" />
	</bean>

</beans>
